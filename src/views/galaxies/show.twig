<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ galaxy.name }} - Galaxy Details</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to bottom, #0d1b2a, #1b263b);
      color: #ffffff;
      padding: 2rem;
      text-align: center;
    }
    h1 {
      color: #00d9ff;
    }
    .info-box {
      margin: 2rem auto;
      padding: 1rem;
      background: rgba(0, 0, 0, 0.3);
      border-radius: 10px;
      width: 80%;
      max-width: 600px;
    }
    img {
      max-width: 100%;
      border: 2px solid #00d9ff;
      border-radius: 8px;
      margin-bottom: 1rem;
    }
    a {
      color: #00d9ff;
      text-decoration: none;
      margin: 0 1rem;
      font-weight: bold;
    }
    a:hover {
      color: #80d8ff;
    }
  </style>
</head>
<body>
  <h1>{{ galaxy.name }}</h1>

  <div class="info-box">
    {% if galaxy.extension %}
      <img src="/images/{{ galaxy.id }}{{ galaxy.extension }}" alt="{{ galaxy.name }}">
    {% else %}
      <p><em>No image available</em></p>
    {% endif %}

    <p><strong>Type:</strong> {{ galaxy.type }}</p>
    <p><strong>Distance from Earth:</strong> {{ galaxy.distanceFromEarth }} light-years</p>
    <p><strong>Number of Stars:</strong> {{ galaxy.starCount }}</p>
    <p><strong>Description:</strong> {{ galaxy.description }}</p>
  </div>

  <div>
    <a href="/galaxies/{{ galaxy.id }}/edit">Edit</a>
    <a href="/galaxies">Back to All Galaxies</a>
  </div>
</body>
</html>
